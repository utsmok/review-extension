# Implementation Plans

Generated by the improve skill on 2026-06-12; reconciled 2026-06-14 against HEAD `b5554b5`.
Each executor: read the plan fully before starting, honor its STOP conditions, and update
your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Batch Export | P2 | S | — | DONE (shipped v0.8.1) |
| 002  | Labs Settings Section | P1 | S | — | DONE (shipped) |
| 003  | Enhanced Recommendation | P2 | M | 002 | DONE (shipped v0.8.1) |
| 004  | Per-Principle Summaries | P2 | M | 002 | REJECTED (added `8aed436`, removed `d410513` — redundant with per-question notes) |
| 005  | Smart Review Templates | P3 | M | — | DONE (shipped v0.8.1 as tool-profiles) |
| 006  | Enforce quality gates (coverage/lint/format) | P1 | S | — | TODO |
| 007  | Harden session lifecycle (switch guard + delete/import errors) | P1 | S | — | TODO |
| 008  | Re-sanitize imported archive HTML | P1 | S | — | TODO |
| 009  | Codebase hygiene (stale docs + vestigial preferredRubric) | P2 | S | — | TODO |
| 010  | undo-delete union merge + migration robustness | P2 | S | — | TODO |
| 011  | Export pipeline cleanup (font asset, ?raw fidelity, reuse model) | P3 | M | — | TODO |
| 012  | Dependency hygiene (runtime deps + @types/node) | P3 | S | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- **006 first**: it tightens lint to `error` and enforces coverage in CI. All later executors run
  against that stricter bar, so land it before 007–012.
- **007, 008, 009, 012** touch disjoint files (session-lifecycle.ts / capture+export / docs+types+registry+tests /
  package.json) — safe to run in parallel after 006.
- **010, 011** touch disjoint files (session.ts+migrations.ts / html-report.ts+report-heading-font.ts+export-pipeline.ts)
  — safe to run in parallel after the wave above.
- **009 reconciles this index**; if it has already been reconciled (rows 001–005 show real status, 006–012 listed),
  treat Step 7 of plan 009 as a verification, not a rewrite.

## Recommended execution order

1. **006** (Quality gates) — establishes the verification bar.
2. **007 + 008 + 009 + 012** in parallel — disjoint files.
3. **010 + 011** in parallel — disjoint files.
4. Full quality gate (`pnpm typecheck && pnpm check && pnpm test:coverage && pnpm build`).
5. Roborev review pass on the commits; fix surfaced issues.

## Findings considered and rejected

- **Side-by-side comparison report**: high product value but L effort. Deferred — now partly addressed by
  Direction A (in-extension comparison).
- **Multi-reviewer governance (§10)**: explicitly scoped out in north star. Not worth planning until institutional adoption.
- **Rubric variant system**: abandoned per north star §10. `preferredRubric` is vestigial (removed in plan 009).
- **AI-assisted scoring**: trust paradox for a tool that evaluates AI trustworthiness. Not appropriate.
- **Critical fail flags (§8)**: valuable but requires data model extension + IDB migration. Deferred.
- **`pnpm audit` critical/high (shell-quote, tmp, esbuild)**: all dev-only transitive via `wxt`, zero shipped-extension
  exposure; resolve when wxt updates its tree. Not worth a plan.
- **Zustand immutable array copies on mutation**: idiomatic Zustand, no measured bottleneck. Not worth a plan.
- **`.editorconfig` / pre-commit hooks**: Biome owns formatting; nice-to-have, not load-bearing.
- **`tools/` as dead weight**: superseded by Direction B (productize the measurement scripts as a CI gate).
